{
  "version": 2,
  "builds": [
    {
      "src": "pubspec.yaml",
      "use": "@tommyers/vercel-flutter",
      "config": { "buildArgs": ["--release"] }
    }
  ],
  "routes": {
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
}